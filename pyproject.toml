[project]
name = "paper-synth"
version = "0.1.0"
description = "Offline-first literature synthesis tool for aggregating findings across research abstracts"
requires-python = ">=3.11"
license = "MIT"
authors = [{name = "Sean Liu", email = "sean.t.liu@gmail.com"}]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.8"]

[project.scripts]
paper-synth = "paper_synth.cli:main"

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
include = ["paper_synth*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"

[tool.ruff]
target-version = "py311"
line-length = 99
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
